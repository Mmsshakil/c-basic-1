#include <iostream>
using namespace std;
int main()
{

    // int x;
    // char s[100];
    string s;
    cin >> s;

    // cin.ignore(); // when we will face using getline or fgets
    // cin >> x;

    // cin >> s;           // if there is not any space in string
    // fgets(s, 100, stdin);  // as like c language
    // cin.getline(s, 100); // we always use this when with space

    // cout << x << endl;
    cout << s << endl;
    return 0;
}